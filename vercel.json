{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm db:generate && pnpm turbo run build --filter=@futuristic/api...",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}

